{
    "$schema": "https://turbo.build/schema.json",
    "globalEnv": ["DATABASE_URL", "SALT_IP_ADDRESS", "NODE_ENV"],
    "tasks": {
        "build": {
            "outputs": ["dist/**", ".next/**"]
        },
        "clean": {
            "cache": false
        },
        "dev": {
            "cache": false
        }
    }
}